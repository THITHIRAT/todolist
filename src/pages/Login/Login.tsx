import LoginForm from "./LoginForm";

const Login = () => {
  return (
    <div className="flex justify-center items-center h-screen w-full overflow-y-hidden bg-background">
      <LoginForm />
    </div>
  );
};

export default Login;
